server {
    listen 80;
    server_name api.carebd.xyz carebd.xyz www.carebd.xyz;

    # Certbot challenge location
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
