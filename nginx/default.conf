server {
    listen 80;
    server_name api.medical-app.xyz medical-app.xyz www.medical-app.xyz;

    # Certbot challenge location
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
